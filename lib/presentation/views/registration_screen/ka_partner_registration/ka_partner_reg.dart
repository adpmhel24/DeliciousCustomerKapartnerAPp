import 'package:flutter/material.dart';

class KaPartnerRegistrationScreen extends StatelessWidget {
  const KaPartnerRegistrationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
